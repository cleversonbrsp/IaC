# Oracle Services Network (Object Storage, etc.) — usado pelo Service Gateway na route table privada.
data "oci_core_services" "all_oci_services" {
  filter {
    name   = "name"
    values = ["All .* Services In Oracle Services Network"]
    regex  = true
  }
}
